{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "cryptest",
            "program": "${workspaceFolder}/bin/vk_test",
            "args": [],
            "cwd": "${workspaceFolder}/build"
        }
    ]
}
